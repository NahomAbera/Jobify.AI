name: jobify_ai_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase packages for authentication and Firestore
  firebase_core: ^2.13.0
  firebase_auth: ^4.6.0
  cloud_firestore: 4.17.5
  
  # Google sign-in for Firebase
  google_sign_in: ^6.2.0
  
  # For material icons and design
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png  # Path to your logo image

